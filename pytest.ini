[pytest]
# Scope collection to the real test suite. The committed duplicate under
# supptracker-improved/ has its own app.py that loads data at import time and
# aborts collection; keep pytest out of it and other heavy/generated dirs.
testpaths = tests
norecursedirs = supptracker-improved node_modules dist .venv node_portable expanded_* __pycache__
